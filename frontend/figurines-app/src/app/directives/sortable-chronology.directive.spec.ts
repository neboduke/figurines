import { SortableChronologyDirective } from './sortable-chronology.directive';

describe('SortableDirective', () => {
  it('should create an instance', () => {
    const directive = new SortableChronologyDirective();
    expect(directive).toBeTruthy();
  });
});
