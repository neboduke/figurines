import { SortableLocationDirective } from './sortable-location.directive';

describe('SortableLocationDirective', () => {
  it('should create an instance', () => {
    const directive = new SortableLocationDirective();
    expect(directive).toBeTruthy();
  });
});
