import { LiteratureSortable } from './literature-sortable';

describe('LiteratureSortable', () => {
  it('should create an instance', () => {
    expect(new LiteratureSortable()).toBeTruthy();
  });
});
